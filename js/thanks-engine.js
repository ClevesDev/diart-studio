// DIAR STUDIO - Thanks Page Engine v1.0
import { initTechIcons, applyGlobalConfig } from './shared-ui.js';
import { CONFIG } from './config.js';

document.addEventListener('DOMContentLoaded', () => {
    applyGlobalConfig(CONFIG);
    initTechIcons();
});
